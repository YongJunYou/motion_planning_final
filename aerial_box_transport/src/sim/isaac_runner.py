"""M6: Isaac Sim runner. Implemented in Step 2, gated on the teammate scene.

Extends this project's existing direct-articulation harness
(../motion_planning_final/main.py) to replay a planned reference (results/*.npz),
add the box, command base wrench plus arm joints (gRITE-in-sim is a later
refinement), and log metrics. See ROADMAP.md, Track B.
"""


def run(*args, **kwargs):
    raise NotImplementedError("M6 Isaac runner is implemented in Step 2 (see ROADMAP.md).")
